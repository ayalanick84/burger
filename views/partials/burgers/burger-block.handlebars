<li>
	{{name}}

	<button class="change-eat btn btn-success"  type= "button" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}Munch Time!{{else}}Make Another One!{{/if}}
	</button>

	<button class="delete-burger btn btn-danger" type= "button" data-id="{{this.id}}">DELETE!</button>
</li>
